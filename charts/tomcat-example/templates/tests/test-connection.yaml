apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tomcat-example.fullname" . }}-test-connection"
  labels:
{{ include "tomcat-example.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "tomcat-example.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
